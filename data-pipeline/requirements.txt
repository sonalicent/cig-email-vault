# Local development requirements for the email .eml -> Markdown/JSON pipeline.
# Mirrors [project].dependencies and [project.optional-dependencies].dev in pyproject.toml.
# For Lambda runtime, deps are installed from pyproject.toml by the Dockerfile.
#
# Install with:  python -m pip install -r requirements.txt
# Then run:      python -m pytest

# --- Runtime dependencies ---
boto3>=1.34
beautifulsoup4>=4.12
markdownify>=0.12
pdfplumber>=0.11
python-pptx>=0.6.23
openpyxl>=3.1
python-docx>=1.1

# --- Development / test dependencies ---
pytest>=8.0
moto[dynamodb,s3]>=5.0
